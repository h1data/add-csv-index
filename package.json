{
  "name": "add-csv-digits",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node24 --outfile=dist/index.js"
  },
  "dependencies": {
    "@actions/core": "^1.1.0",
    "csv-parser": "^3.2.0",
    "csv-writer": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.0"
  }
}
